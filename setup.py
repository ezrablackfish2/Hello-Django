from setuptools import setup

setup(
    name='freeze',
    version='0.1',
    description='A simple package that lists all of the Python packages that are currently installed in a virtual environment.',
    url='https://github.com/bard/freeze',
    author='Bard',
    author_email='bard@example.com',
    packages=['freeze'],
    python_requires='>=3.7',
)
